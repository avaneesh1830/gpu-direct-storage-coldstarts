# gpu-direct-storage-coldstarts
Optimizing LLM serverless cold starts and inference runtimes using NVIDIA GPUDirect Storage (GDS), CRIU container snapshots, and CUDA Checkpoint/Restore. Benchmarks and implementation for sub-second GPU state initialization.
